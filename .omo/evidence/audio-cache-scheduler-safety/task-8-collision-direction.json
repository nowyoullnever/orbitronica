{
  "task": 8,
  "repair": "collision-driven direction refresh",
  "status": "pass",
  "scenario": "Canvas physics emits a direction flip; App publishes an immutable next scene through readiness refresh; the collector emits the exact reverse request and existing reverse prewarm installs it before playback.",
  "commands": [
    "node --experimental-strip-types --test test/sceneAudioReadiness.test.ts test/appSceneContract.test.ts",
    "npx tsc -b --force",
    "git diff --check"
  ]
}
