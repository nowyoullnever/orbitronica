node_modules
dist
dist-electron
release
*.log
.omx
.omo/
*.tsbuildinfo

# Mapo Flower Island font — license prohibits redistribution, so it is never
# committed. `npm install` (or `npm run fetch:font`) downloads it from source.
MapoFlowerIsland.woff
MapoFlowerIsland.otf
